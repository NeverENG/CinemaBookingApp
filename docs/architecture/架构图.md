# LTerm 系统架构图

> 版本：v0.1  
> 用途：实验报告、项目答辩和源码导读。图中的组件、角色和数据表均以当前项目实现为准，不额外引入 Kafka、Redis 或 Kubernetes。

## 1. 图表说明

本项目建议使用四张图说明不同问题：

| 图表 | 说明重点 | 适合回答的问题 |
| --- | --- | --- |
| 服务运行架构图 | 系统运行时组件和请求流向 | 浏览器请求如何到达数据库？ |
| 代码依赖关系图 | 前后端代码分层和依赖方向 | 为什么 service 不直接写 SQL？ |
| 数据关系图 | 核心表、外键和业务关联 | 一次购票涉及哪些数据？ |
| 服务提供架构图 | 三类管理角色和用户得到的业务能力 | 每个角色可以使用哪些服务？ |

四张图不要合并成一张。运行架构回答“系统怎么运行”，依赖关系回答“代码怎么组织”，数据关系回答“数据怎么关联”，服务提供架构回答“系统为谁提供什么能力”。

## 2. 服务运行架构图

该图描述 Docker Compose 启动后的实际运行关系。前端容器由 Nginx 提供 React 静态文件，并把 `/api` 请求代理到后端容器；后端通过 PostgreSQL Repository 访问数据库。

```mermaid
flowchart TB
    subgraph CLIENT[访问端]
        USER_BROWSER[用户浏览器]
        ADMIN_BROWSER[管理端浏览器]
    end

    subgraph FRONTEND[frontend 容器]
        NGINX[Nginx\nReact 静态资源\n/api 同源反向代理]
    end

    subgraph BACKEND[backend 容器]
        ROUTER[Gin Router]
        MIDDLEWARE[CORS\nToken Bucket 限流\nJWT + RBAC]
        HANDLER[HTTP Handler]
        SERVICE[Application Service\n订单 / 支付 / 退款 / 管理 / 看板]
        REPOSITORY[PostgreSQL Repository]
        JOB[后台定时任务\n订单超时 / 回调重试 / 对账]
        MAILER[邮件适配器]
    end

    DATABASE[(PostgreSQL\n业务表 / 交易流水 / 审计日志)]
    SMTP[SMTP 邮箱服务\n可选 QQ SMTP]

    USER_BROWSER --> NGINX
    ADMIN_BROWSER --> NGINX
    NGINX -->|页面资源| USER_BROWSER
    NGINX -->|页面资源| ADMIN_BROWSER
    NGINX -->|/api/v1| ROUTER
    ROUTER --> MIDDLEWARE --> HANDLER --> SERVICE
    SERVICE --> REPOSITORY --> DATABASE
    JOB -->|只调用用例| SERVICE
    SERVICE --> MAILER --> SMTP
```

### 2.1 运行边界

- `frontend` 和 `backend` 是两个应用容器，`db` 是 PostgreSQL 容器，Compose 负责启动顺序和健康检查。
- `migrate` 容器在后端启动前执行数据库迁移和 Seed；它不是长期运行的业务服务。
- 定时任务和 HTTP 服务运行在同一个 backend 进程中，当前课程版本只启动一个 Worker。
- 支付使用 Mock 支付和 Mock 回调接口，不代表已经接入真实支付平台。
- 邮箱配置 SMTP 后发送真实邮件；未配置 SMTP 时，开发环境返回验证码方便演示。

## 3. 代码依赖关系架构图

该图强调依赖方向。箭头表示“依赖或调用”，虚线表示“接口由适配器实现”。`wire` 是组合根，负责把接口和具体实现装配起来。

```mermaid
flowchart LR
    subgraph WEB[前端]
        PAGE[pages 页面]
        HOOK[features/*\n业务 Hook]
        API[services/api\n领域 API]
        AXIOS[services/http\nAxios 拦截器]
        PAGE --> HOOK --> API --> AXIOS
    end

    subgraph APP[后端应用层]
        ROUTER[app/server/http/router]
        MW[app/server/http/middleware]
        HANDLER[app/server/http/handler]
        SVC[app/server/service\n用例编排]
        JOBS[app/job\n周期任务]
        ROUTER --> MW --> HANDLER --> SVC
        JOBS --> SVC
    end

    subgraph CORE[核心层]
        PORT[core/port\n仓储与事务接口]
        DOMAIN[core/domain\n实体与状态机]
        PORT --> DOMAIN
    end

    subgraph INFRA[基础设施层]
        REPO[infra/database/postgres\nRepository 实现]
        CONFIG[infra/config\n环境配置]
        TOOL[internal/pkg\nJWT / UID / Mailer]
        REPO -.实现.-> PORT
        REPO --> DOMAIN
    end

    DB[(PostgreSQL)]
    WIRE[app/wire\n依赖注入组合根]

    AXIOS --> ROUTER
    SVC --> PORT
    SVC --> DOMAIN
    REPO --> DB
    WIRE --> ROUTER
    WIRE --> SVC
    WIRE --> REPO
    WIRE --> CONFIG
    WIRE --> TOOL
```

### 3.1 依赖规则

1. `handler` 负责绑定参数、调用 service 和映射错误，不直接访问数据库。
2. `service` 依赖 `core/port` 接口和 `core/domain` 规则，不依赖具体 PostgreSQL 类型。
3. `infra/database/postgres` 实现 `core/port`，SQL 和 GORM 细节留在基础设施层。
4. `job` 只调用 service 用例，不绕过状态机直接修改数据。
5. 前端页面只使用业务 Hook；Axios、错误分类、Query Key 和缓存刷新集中在 server 层处理。

## 4. 数据关系架构图

下图是当前数据库的核心 ER 图。带有 `logical` 的关系表示业务关联但没有直接使用数据库外键，例如邮箱验证码通过 `email` 关联用户、票房流水通过业务单号关联支付或退款事件。

```mermaid
erDiagram
    USERS {
        BIGINT id PK
        VARCHAR email UK
        BIGINT membership_level_id FK
        INT points_balance
        VARCHAR status
    }
    MEMBERSHIP_LEVELS {
        BIGINT id PK
        VARCHAR level_code UK
        INT min_total_points
        INT discount_bp
    }
    MEMBERSHIP_LEVEL_LOGS {
        BIGINT id PK
        BIGINT user_id FK
        BIGINT to_level_id FK
        VARCHAR change_type
    }
    EMAIL_VERIFICATION_CODES {
        BIGINT id PK
        VARCHAR email
        VARCHAR code_hash
        TIMESTAMPTZ expires_at
    }
    PASSWORD_RESET_CODES {
        BIGINT id PK
        VARCHAR email
        VARCHAR code_hash
        TIMESTAMPTZ expires_at
    }
    CINEMAS {
        BIGINT id PK
        VARCHAR name
        VARCHAR city
        VARCHAR status
    }
    HALLS {
        BIGINT id PK
        BIGINT cinema_id FK
        JSONB seat_layout_json
        VARCHAR status
    }
    SEATS {
        BIGINT id PK
        BIGINT hall_id FK
        VARCHAR seat_no
        VARCHAR type
        VARCHAR status
    }
    MOVIES {
        BIGINT id PK
        VARCHAR title
        INT duration_minutes
        VARCHAR status
    }
    SHOW_SESSIONS {
        BIGINT id PK
        BIGINT cinema_id FK
        BIGINT hall_id FK
        BIGINT movie_id FK
        TIMESTAMPTZ start_time
        TIMESTAMPTZ end_time
        BIGINT base_price_cents
        VARCHAR status
    }
    SEAT_LOCKS {
        BIGINT id PK
        BIGINT session_id FK
        BIGINT seat_id FK
        BIGINT user_id FK
        VARCHAR order_no
        VARCHAR status
        TIMESTAMPTZ expires_at
    }
    ORDERS {
        VARCHAR order_no PK
        BIGINT user_id FK
        BIGINT session_id FK
        BIGINT cinema_id
        BIGINT movie_id
        BIGINT paid_cents
        VARCHAR status
        TIMESTAMPTZ paid_at
    }
    ORDER_ITEMS {
        BIGINT id PK
        VARCHAR order_no FK
        BIGINT session_id
        BIGINT seat_id FK
        VARCHAR ticket_no UK
        TIMESTAMPTZ used_at
    }
    PAYMENT_TRANSACTIONS {
        VARCHAR transaction_no PK
        VARCHAR biz_no
        BIGINT user_id FK
        BIGINT amount_cents
        VARCHAR status
        TIMESTAMPTZ paid_at
    }
    PAYMENT_CALLBACKS {
        BIGINT id PK
        VARCHAR event_id UK
        VARCHAR transaction_no
        VARCHAR status
        INT retry_count
    }
    REFUNDS {
        BIGINT id PK
        VARCHAR refund_no UK
        VARCHAR order_no FK
        BIGINT user_id FK
        BIGINT amount_cents
        VARCHAR status
    }
    COUPON_TEMPLATES {
        BIGINT id PK
        VARCHAR name
        VARCHAR type
        INT total_qty
        VARCHAR status
    }
    USER_COUPONS {
        BIGINT id PK
        VARCHAR coupon_no UK
        BIGINT template_id FK
        BIGINT user_id FK
        VARCHAR order_no
        VARCHAR status
    }
    POINTS_LEDGER {
        BIGINT id PK
        BIGINT user_id FK
        INT change_points
        VARCHAR biz_type
        VARCHAR biz_no
    }
    ROLES {
        BIGINT id PK
        VARCHAR code UK
        JSONB permissions_json
    }
    ADMINS {
        BIGINT id PK
        VARCHAR username UK
        BIGINT role_id FK
        BIGINT cinema_id FK
        VARCHAR status
    }
    OPERATION_LOGS {
        BIGINT id PK
        BIGINT admin_id FK
        VARCHAR action
        VARCHAR target_id
    }
    BANNERS {
        BIGINT id PK
        VARCHAR title
        VARCHAR image_url
        BOOLEAN enabled
    }
    BOX_OFFICE_LEDGER {
        BIGINT id PK
        VARCHAR biz_type
        VARCHAR biz_no
        DATE stat_date
        BIGINT cinema_id
        BIGINT movie_id
    }
    DAILY_BOX_OFFICE {
        BIGINT id PK
        DATE stat_date
        BIGINT cinema_id
        BIGINT movie_id
        BIGINT gross_cents
        BIGINT refund_cents
    }
    LOGIN_GUARDS {
        BIGINT id PK
        VARCHAR scope
        VARCHAR username
        INT failure_count
    }

    MEMBERSHIP_LEVELS ||--o{ USERS : grants
    USERS ||--o{ MEMBERSHIP_LEVEL_LOGS : changes
    MEMBERSHIP_LEVELS ||--o{ MEMBERSHIP_LEVEL_LOGS : target
    USERS ||--o{ EMAIL_VERIFICATION_CODES : email_logical
    USERS ||--o{ PASSWORD_RESET_CODES : email_logical
    CINEMAS ||--o{ HALLS : contains
    HALLS ||--o{ SEATS : contains
    MOVIES ||--o{ SHOW_SESSIONS : schedules
    CINEMAS ||--o{ SHOW_SESSIONS : runs
    HALLS ||--o{ SHOW_SESSIONS : hosts
    USERS ||--o{ SEAT_LOCKS : locks
    SHOW_SESSIONS ||--o{ SEAT_LOCKS : reserves
    SEATS ||--o{ SEAT_LOCKS : occupies
    USERS ||--o{ ORDERS : places
    SHOW_SESSIONS ||--o{ ORDERS : sells
    ORDERS ||--o{ ORDER_ITEMS : contains
    SEATS ||--o{ ORDER_ITEMS : snapshots
    ORDERS ||--o| PAYMENT_TRANSACTIONS : pays
    PAYMENT_TRANSACTIONS ||--o{ PAYMENT_CALLBACKS : receives
    ORDERS ||--o| REFUNDS : refunds
    USERS ||--o{ REFUNDS : requests
    COUPON_TEMPLATES ||--o{ USER_COUPONS : instantiates
    USERS ||--o{ USER_COUPONS : owns
    ORDERS ||--o| USER_COUPONS : consumes
    USERS ||--o{ POINTS_LEDGER : writes
    ROLES ||--o{ ADMINS : assigns
    CINEMAS ||--o{ ADMINS : scopes
    ADMINS ||--o{ OPERATION_LOGS : writes
    CINEMAS ||--o{ BOX_OFFICE_LEDGER : aggregates
    MOVIES ||--o{ BOX_OFFICE_LEDGER : aggregates
    CINEMAS ||--o{ DAILY_BOX_OFFICE : reports
    MOVIES ||--o{ DAILY_BOX_OFFICE : reports
```

### 4.1 一次购票的数据流

```mermaid
flowchart LR
    U[用户] --> S[show_sessions 场次]
    S --> L[seat_locks 临时锁座]
    L --> O[orders 待支付订单]
    O --> P[payment_transactions 支付交易]
    P --> C[payment_callbacks 支付回调]
    C --> I[order_items 票据明细]
    C --> G[points_ledger 积分流水]
    C --> B[box_office_ledger / daily_box_office 票房]
    O --> UC[user_coupons 优惠券状态]
```

核心约束是：订单保存座位和价格快照，支付回调完成出票；座位锁的部分唯一索引防止同一场次同一座位被重复占用；支付回调、积分流水和票房流水通过唯一业务号实现幂等。

## 5. 服务提供架构图

该图从使用者角度描述系统提供的业务能力。它不是代码包依赖图，而是 SaaS 权限和业务服务目录。

```mermaid
flowchart LR
    subgraph ROLES[服务使用角色]
        USER[普通用户]
        SUPER[超级管理员]
        CINEMA[影院管理员]
        FINANCE[财务账号]
    end

    subgraph SERVICES[平台提供的业务服务]
        AUTH[身份认证服务\n用户登录 / 管理员登录\n邮箱验证 / 找回密码]
        DISCOVERY[电影发现服务\n首页热映 / 搜索 / 影院 / 场次]
        BOOKING[购票交易服务\n选座 / 锁座 / 下单 / Mock 支付\n出票 / 退票 / 改签]
        CONTENT[平台内容服务\n影片 / Banner / 影院资料]
        CINEMA_OPS[影院运营服务\n影厅座位布局 / 排期 / 票价]
        TICKET[票券服务\n票码查询 / 核销]
        MEMBER[会员营销服务\n会员等级 / 积分 / 优惠券]
        DASHBOARD[经营分析服务\n票房趋势 / 影院排行 / 影片排行\n对账与数据聚合]
    end

    USER --> AUTH
    USER --> DISCOVERY
    USER --> BOOKING
    USER --> MEMBER

    SUPER --> AUTH
    SUPER --> CONTENT
    SUPER --> CINEMA_OPS
    SUPER --> TICKET
    SUPER --> DASHBOARD

    CINEMA --> AUTH
    CINEMA --> DISCOVERY
    CINEMA --> CINEMA_OPS
    CINEMA --> TICKET
    CINEMA --> DASHBOARD

    FINANCE --> AUTH
    FINANCE --> DASHBOARD
```

### 5.1 SaaS 角色边界

| 角色 | 服务范围 | 数据范围 | 关键限制 |
| --- | --- | --- | --- |
| `USER` | 电影发现、选座购票、支付、退款、改签、积分 | 公开内容和自己的订单 | 不能访问管理接口 |
| `SUPER_ADMIN` | 平台内容、影院运营、账号、票券和全局数据 | 全平台、全部影院 | 关键变更写入审计日志 |
| `CINEMA_ADMIN` | 本影院影厅、座位、排期、票价、核销和看板 | `admins.cinema_id` 绑定影院 | 不能改影片、Banner、优惠券和管理员账号 |
| `FINANCE` | 票房和经营数据查询 | 全局或绑定影院 | 只读，不能排期、改价或核销 |

### 5.2 服务和接口入口

| 业务服务 | 当前接口入口 |
| --- | --- |
| 身份认证 | `/api/v1/auth/*`、`/api/v1/admin/auth/login` |
| 电影发现 | `/api/v1/home`、`/api/v1/movies`、`/api/v1/cinemas`、`/api/v1/sessions` |
| 购票交易 | `/api/v1/orders/*`、`/api/v1/payments/*`、`/api/v1/refunds/*`、`/api/v1/orders/*/change` |
| 平台内容 | `/api/v1/admin/movies`、`/api/v1/admin/banners`、`/api/v1/admin/cinemas` |
| 影院运营 | `/api/v1/admin/halls`、`/api/v1/admin/sessions` |
| 票券核销 | `/api/v1/admin/tickets/verify` |
| 会员营销 | `/api/v1/me/points`、`/api/v1/coupons/redeemable`、`/api/v1/admin/coupons/*` |
| 经营分析 | `/api/v1/admin/dashboard/box-office/*` |

## 6. 报告使用建议

- 系统总体设计章节放“服务运行架构图”。
- 软件结构设计章节放“代码依赖关系架构图”。
- 数据库设计章节放“数据关系架构图”和“一次购票的数据流”。
- 权限设计或系统功能章节放“服务提供架构图”和角色边界表。
- Word 中建议每张图单独编号，例如“图 4-1 LTerm 服务运行架构图”，图下补充 3 到 5 句图解，避免只贴图片没有说明。
